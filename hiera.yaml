---
version: 5

defaults:
  datadir: 'data'
  data_hash: 'yaml_data'

hierarchy:
  - name: "Nodes"
    path: "nodes/%{trusted.certname}.yaml"

  - name: "Defaults"
    glob: "defaults/*.yaml"

  - name: "Common"
    path: "common.yaml"
